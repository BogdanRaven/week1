# Exercise 2 — URL Validation with Specifications

## Prompt

The prompt specified the function name `validate_url`, a `(bool, str)` return type, HTTP/HTTPS-only protocols, domain and port validation, paths/query strings, descriptive errors, type hints, examples, and empty/`None`/malformed-input handling.

## Result

The specifications improved output structure and error reporting for every model. Sol passed every required URL at every effort level. Terra's medium-effort answer contained a missing import and failed at runtime, demonstrating why generated code must be executed rather than judged only by appearance.

See [the detailed Part 2 report](../homework_part2.md#exercise-2-improved-with-specifications) and [the normalized results](../results/part2_url_validation.md).
